# Local config — secrets, paths
config.env
.env

# Per-issue worktrees created by Symphony
_workspaces/

# Runtime copy of the promatch repo. The committed source lives in
# promatch.template/ — bootstrap.sh copies it here and `git init`s the copy.
# This keeps the outer repo nesting-free and ships a clean template to
# every recipient on `git clone`.
/promatch/

# Local task store (offline-demo). Bootstrap re-creates it from
# scripts/seed-local.py — the committed source of truth is the seed script.
/tasks.json
/tasks.json.lock

# Logs
symphony.log
*.log

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# OS
.DS_Store
